# Credential Information

Bạn có thể sử dụng `AWS IAM` hoặc `Azure Managed Service Identity` để cung cấp credential cho terraform. Với các services này ta có thể kết nối tới API mà không cần expose các thông tin nhạy cảm trong terraform config.
